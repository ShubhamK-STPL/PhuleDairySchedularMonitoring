name: Selenium Hangfire Status Job

on:
  push:
    branches:
      - '**'           # ✅ Trigger on any branch
    paths:
      - '**'           # ✅ Trigger on any file change
  schedule:
    # ✅ Runs daily at 3:30 AM UTC, which is 9:00 AM IST
    - cron: "30 3 * * *"
    - cron: "58 3 * * *"
  workflow_dispatch:        # ✅ Manual trigger option

jobs:
  run-tests-and-notify:
    runs-on: ubuntu-latest
    steps:
      # ✅ Checkout source code
      - name: Checkout Repository
        uses: actions/checkout@v3

      # ✅ Setup Java 17
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # ✅ Cache Maven Dependencies
      - name: Cache Maven Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven

      # ✅ Install Google Chrome for Selenium
      - name: Install Google Chrome
        uses: browser-actions/setup-chrome@v1

      # ✅ Run Selenium Tests (PhuleDairyJobStatus also sends Teams notification)
      - name: Run Tests and Send Teams Notification
        run: |
          set +e
          echo "🚀 Running Selenium tests..."
          mvn clean test -DsuiteXmlFile=testng.xml
          RESULT=$?
          if [ $RESULT -eq 0 ]; then
            echo "✅ Tests Passed"
          else
            echo "❌ Tests Failed (Teams notification will still be sent)"
          fi
          echo "ℹ️ Running PhuleDairyJobStatus to send Teams update..."
          mvn -Dtest=PhuleDairyJobStatusMD test
          exit $RESULT   # Keep test status as final exit code

    env:
      TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
      HANGFIRE_URL: ${{ secrets.HANGFIRE_URL }}
